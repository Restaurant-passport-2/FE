import React, { useState } from "react";
import { fetchRecommendations } from '../actions/actions';
import { connect } from "react-redux";

const LocationForm = props => {
    return (
        <form>
            <legend>Search By Location</legend>
        </form>
    )
}
export default LocationForm;

